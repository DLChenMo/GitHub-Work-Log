今天主要完成的任务汇报：
  今天主要是看了Blockchain based Secure Cloud Storage and Access Control for IoT Data（19年）论文，这篇论文主要是应用了收敛加密来加密数据，
  基于令牌对数据进行访问。数据加密采用公开加密和私有加密两种加密方式。在公开加密的方式中，标签采用双重哈希值来对数据进行去重，
  即先对文件进行哈希，再对该哈希值进行哈希，用于判定服务器中是否存储了该文件。私有加密方式，标签是由公司的私密信息来生成，
  由于每个公司的私密信息不同，因而无法对重复信息进行检索。文件的访问控制和审计主要是利用区块链来完成的。但是论文中只是描述了一下，
  并没有用具体算法来进行说明。
  同时了解了一下收敛加密，主要是利用文件的哈希值对文件进行加密，感觉这种加密方式存在问题，只有拥有文件的用户才可以访问，否则无法进行解密。
  同时密钥存储也会存在问题。
