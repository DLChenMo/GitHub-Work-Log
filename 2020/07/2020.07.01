今天主要完成的任务汇报：
    今天主要是在继续看昨天汇报的Identity-Based Data Outsourcing with Comprehensive Auditing in Clouds（16年TIFS）论文。看了整个方案，
利用代理签名的思想，数据拥有者生成授权给代理者，代理者利用自身的私钥信息对数据拥有者的数据进行签名，用于完整性审计。
这样的签名中不在包含数据拥有者的身份信息，为了保证签名的合法性，TPA在验证数据的完整性时，还是需要对数据拥有者的授权进行验证，
保证数据是合法的授权者上传的。这样的代理签名方式和之前的代理签名感觉相差并不是太多，只是在授权签名中不在包含数据拥有者的身份信息，
但是在验证过程中还是需要验证的。
