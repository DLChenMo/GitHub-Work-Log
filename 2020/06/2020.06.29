今天主要完成的任务汇报：
    今天主要是在看昨天的汇报的IoT的那篇论文，发现论文中对数据进行盲化操作。但是盲化因子在审计过程中是需要知道的。因此，可以认为审计者是能够恢复出原始的数据的。
然后就是在考虑云服务器生成秘钥，并生成环秘钥的目的是什么？而且在验证过程中，并对这个环秘钥进行了验证。在考虑这个细节，因为我们主要是验证数据的完整性，并不需要验证云服务器的秘钥信息。
如果是避免让审计者知道数据是对应存储在哪一个云服务器上的话，那应该也是不需要验证云服务器的秘钥信息的。还在思考这个问题。
