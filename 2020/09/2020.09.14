今天主要完成的任务汇报：
   今天主要是招了一篇论文Efficient audit protocol supporting virtual nodes in cloud storage（20年的一个特刊论文），利用变色龙哈希函数来构建merkle树，实现数据的动态审计。
看了方案部分，觉得并没有太大的创新点。在merkle树的生成过程中，对树的根值进行签名，同时生成数据块的审计标签。但是我觉得存在一定的问题，因为看之前的论文，
在审计过程中都是对merkle树进行重构进行判定数据是否被篡改。但是这篇论文的审计方式只是对根值的签名和数据的审计标签进行判定，觉得如果利用审计标签就能够进行判定，那就没必要对根值也进行判定了。
因为静态的审计过程都是利用审计标签进行判定数据是否被篡改。
