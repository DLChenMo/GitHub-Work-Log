今天主要完成的工作汇报：
  今天主要继续看论文接下来的算法，主要看了错误检测算法，文件产生算法和密钥更新算法和文件访问和用户信息追踪算法。
  对于错误检测算法主要流程如下：
    因为群用户并不是完全诚实的执行算法，可能破坏会话密钥的建立。因此需要错误检测算来来进行检测。
    首先群内用户会将子集和签名发给群管理员，群管理员计算会话密钥，并将这些信息发给用户，用户进行验证。
    如果所有用户都诚实执行算法，则建立会话密钥，一旦用户验证不通过，则用户就会向群管理员发送错误报告，
    群管理员先验证用户提交的信息是不是正确的，如果不正确，则判定用户为恶意用户。如果通过验证，表明用户提交的信息是正确的，
    则群管理员则会验证别的用户给他发的消息计算出来的信息和他提交的信息是不是相同的，如果不同，则判定用户为恶意用户，如果相同，
    则验证群内其他成员，判定恶意用户是否为其他用户。从而检测出来恶意用户信息。
 对于文件产生和密钥更新算法的流程如下：
    用户利用会话密钥加密数据，交给群管理员，群管理员利用RSA算法对用户数据进行加密，保证数据的机密性和控制数据的访问。
    当有用户需要访问共享数据时，先向群管理员发送请求，群管理员验证其是否为合法用户，如果是，则向群发送授权信息，群管理员计算密钥
    和数据一起发给用户，用户利用密钥来进行解密，使用数据。密钥更新也只是群管理员进行密钥更新，管理员更新密钥之后，对之前的数据进行
    重加密，这样便控制了非法用户不能访问数据了。
 算法大致内容看懂了，但还有很多细节需要去进一步理解。
