今天主要完成的工作汇报：
  今天主要是上课。然后在细看了一下在云计算环境下实现群数据共享的匿名性和追踪论文的前两部算法，系统初始化和会话密钥生成这两个部分的内容。
  对于系统初始化，主要包含如下三部分内容：
  1、初始化系统参数。
  2、用户注册，群用户依据自身身份信息向群管理员进行注册。群管理员计算其私钥返回给用户。
  3、SBIBD结构的建立，不过这部分具体怎么实现的还没看懂，主要作用就是建立(n,kn+1,1)这样的一个块。其中n代表用户数目，kn表示一个素数，
      1应该表示在集合中任意两个不相同的元素对出现在子集中的次数为一次。应该是这个含义，具体细节我等看完论文的方案在仔细看。
   
   对于系统初始化算法也主要分成两部分组成，先要构建群内用户的数量为n个用户，应为基于SBIBD结构的话，需要n个用户。一旦组内用户数目不够n个的话，
   群管理员就会指定一定数目的志愿者来帮助群用户建立会话密钥，但这些志愿者最后并不会拥有会话密钥。其中主要算法如下：
   1、群内的每个成员会调用算法产生消息的签名。将消息和签名与同组的用户进行交换，每个用户会接收到一定数量的信息，对于这个定量，我还没看明白。
      然后群内成员依据这些信息计算Cj,i，然后用户第二部来计算会话密钥。在此阶段中，志愿者仅仅接收信息，不会发送信息。
   2、每个成员接收到来自其他用户的Cj，i和对应的签名，验证这些信息的正确性之后建立会话密钥。
      在此阶段中，志愿者仅仅发送自己计算的信息，不会接收信息。
  在这两个算法中，还有很多细节方面的东西还没理清楚，还需要在细看。
  
